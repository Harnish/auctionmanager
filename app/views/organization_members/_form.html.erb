<%= form_for(@organization_member) do |f| %>
  <% if @organization_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@organization_member.errors.count, "error") %> prohibited this organization_member from being saved:</h2>

      <ul>
      <% @organization_member.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :organization_id %><br />
    <%= f.text_field :organization_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
